{
  "name": "black-oracle-report",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "Evidence-first BLACK ORACLE research and report runtime",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test dist/**/*.test.js",
    "build": "tsc -p tsconfig.json",
    "verify": "npm run build && node --test dist/**/*.test.js",
    "start": "node dist/start.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
